{
  "name": "face-feature-detection",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "latest",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^6.2.0",
    "wrangler": "^4.76.0"
  }
}
